#!/usr/bin/make -f
# -*- makefile -*-
#
# Configures, builds and installs Bob on Debian systems.

# Uncomment this to turn on verbose mode.
export DH_VERBOSE=1

%:
			dh $@ --with python-virtualenv

override_dh_virtualenv:
	    dh_virtualenv --use-system-packages --verbose

